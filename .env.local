# Keycloak Configuration
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080
NEXT_PUBLIC_KEYCLOAK_REALM=Customer
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=customer-web
NEXT_PUBLIC_KEYCLOAK_DEV_REALM=developer
NEXT_PUBLIC_KEYCLOAK_VENDOR_REALM=vendor
NEXT_PUBLIC_KEYCLOAK_DEV_CLIENT_ID=dev-web
NEXT_PUBLIC_KEYCLOAK_VENDOR_CLIENT_ID=vendor-web

# Keycloak Admin Credentials (for user provisioning)
KEYCLOAK_ADMIN=admin@keycloak.local
KEYCLOAK_ADMIN_PASSWORD=dev_admin_password_2024!

# Enable TOTP requirement for new users
KEYCLOAK_REQUIRE_TOTP=false

# Database Configuration
DB_HOST=localhost
DB_USER=llm_user
DB_PASSWORD=secure_password123
DB_NAME=llm_evaluation
DB_PORT=3307

# MailHog Configuration (for testing email)
SMTP_HOST=localhost
SMTP_PORT=1026

# Google OAuth Configuration
NEXT_PUBLIC_GOOGLE_IDP_ALIAS=google

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Security Configuration
# CSRF Protection (generate a secure random string)
CSRF_SECRET=your-csrf-secret-key

# Session Secret (generate a secure random string)
SESSION_SECRET=your-session-secret-key

# MySQL Root Password (update this with your actual MySQL root password)
MYSQL_ROOT_PASSWORD=root_password_2024!

# Insert sample data during database seeding
INSERT_SAMPLE_DATA=true